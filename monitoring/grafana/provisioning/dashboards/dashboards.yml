apiVersion: 1

providers:
  - name: luexing-dynamictp
    orgId: 1
    folder: 掠星
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
